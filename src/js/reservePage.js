const addNewDateBtn = document.getElementById("add-new-date-btn");
const datesContainer = document.querySelector(".dates-container");

let index = 1;

function handleAddNewDate() {}

addNewDateBtn.addEventListener("click", handleAddNewDate());
